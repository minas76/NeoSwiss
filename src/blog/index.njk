---
layout: base.njk
title: Blog
description: Articles on design, development, and web best practices
---

<section class="section">
  <div class="container">
    <div style="grid-column: span 12;">
      <p class="eyebrow">Articles</p>
      <h1>Blog</h1>
      <p class="lede">Thoughts on Neo-Swiss design, responsive web development, and digital design systems.</p>
    </div>
    
    {% for post in collections.blog %}
      <article class="card scroll-up" style="grid-column: span 6; @media (max-width: 768px) { grid-column: span 6; } @media (max-width: 480px) { grid-column: span 4; }">
        <h3 class="card-title">{{ post.data.title }}</h3>
        <p class="card-body">{{ post.data.description }}</p>
        <footer class="card-footer">
          <span class="date">{{ post.data.date | readableDate }}</span>
          <a href="{{ post.url }}" class="link-arrow">Read →</a>
        </footer>
      </article>
    {% endfor %}
  </div>
</section>
